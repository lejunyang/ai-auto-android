# 项目协作规范

## 语言

- 与用户沟通、任务计划、评审结论和项目文档默认使用简体中文。
- 代码标识符、命令、参数、环境变量、路径、错误码、JSON 字段和协议常量保持其既有英文形式，不为中文化破坏机器可读契约。
- Agent Skills 的说明、流程和安全提示使用简体中文；frontmatter `name` 与命令示例保持稳定。

## 注释

- 所有可注释的手写生产代码、测试、构建配置、CI 和脚本必须包含简洁的中文功能或用途注释。
- Go 使用 package doc，并为导出 API、关键安全边界和复杂流程补充必要说明。
- Kotlin/Java 使用文件或主要类型 KDoc；测试类注释说明被验证的能力、风险或回归场景。
- Gradle、Shell、Node 脚本、Workflow、XML 和其他配置文件在合法位置说明模块用途或非显然约束。
- 注释解释“为什么、用途和边界”，不得逐行复述代码，也不得用无信息量注释凑覆盖。
- 修改行为时同步更新相关注释；新增手写文件必须纳入 `make comments-check`。

## 排除项

- JSON、JSON Schema、协议 fixture、生成文件、依赖锁文件、Gradle Wrapper、构建产物、缓存和二进制不强制添加注释。
- 用户可见字符串不代替代码注释；自动检查只接受对应语言注释语法中的中文。

## 验证

- 运行 `make comments-check` 检查受管手写文件的中文注释覆盖。
- 提交前按改动范围运行定向测试，并在最终验收运行 `make test`、`make verify`、`make build` 与 Android 单测、lint 和构建任务。
