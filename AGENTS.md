# 项目协作规范

## 语言

- 与用户沟通、任务计划、评审结论和项目文档默认使用简体中文。
- 代码标识符、命令、参数、环境变量、路径、错误码、JSON 字段和协议常量保持其既有英文形式，不为中文化破坏机器可读契约。
- Agent Skills 的说明、流程和安全提示使用简体中文；frontmatter `name` 与命令示例保持稳定。

## 注释

- 所有可注释的手写生产代码、测试、构建配置、CI 和脚本必须包含简洁的中文功能或用途注释。
- Go 使用 package doc，并为导出 API、关键安全边界和复杂流程补充必要说明。
- Kotlin/Java 使用文件或主要类型 KDoc；测试类注释说明被验证的能力、风险或回归场景。
- Gradle、Shell、Node 脚本、Workflow、XML 和其他配置文件在合法位置说明模块用途或非显然约束。
- 注释解释“为什么、用途和边界”，不得逐行复述代码，也不得用无信息量注释凑覆盖。
- 修改行为时同步更新相关注释；新增手写文件必须纳入 `make comments`。

## 排除项

- JSON、JSON Schema、协议 fixture、生成文件、依赖锁文件、Gradle Wrapper、构建产物、缓存和二进制不强制添加注释。
- 用户可见字符串不代替代码注释；自动检查只接受对应语言注释语法中的中文。

## 验证

- 运行 `make comments` 检查受管手写文件的中文注释覆盖。
- 提交前按改动范围运行定向测试，并在最终验收运行 `make test`、`make verify`、`make build` 与 Android 单测、lint 和构建任务。

## 规格与路线图

- 当前 MVP 的完成状态以 `.trae/specs/build-ai-android-automation-mvp/` 下的 `spec.md`、`tasks.md`、`checklist.md` 和 append-only `progress.md` 为准。
- 已完成任务必须有实现、测试或设备证据后才能勾选，不得把设计、编译成功或计划中的能力描述为已经交付。
- MVP 之后尚未实现的工作统一写入 `docs/next-phase-tasks.md`；实现时再按规格流程拆入对应任务，不回写或篡改历史验收结论。
- MVP 后工作实施时必须创建独立 change spec；未开始或未验收的路线图任务不得复制进已完成 MVP 的 `tasks.md` 或 `checklist.md`。

## Git 与 Worktree

- 开工前运行 `git status --short --branch` 和 `git worktree list --porcelain`，识别用户改动、未提交文件、现有分支和并行 worktree。
- 相互独立的实现任务使用独立分支和独立 worktree；单个提交保持单一职责并使用 Conventional Commit。
- 禁止强制移除 dirty worktree，禁止删除未合并或未确认等价合入的分支，禁止用 reset、checkout 或 clean 覆盖未知改动。
- 任务合入并确认 worktree 干净后，使用非强制 `git worktree remove` 清理，再运行 `git worktree prune`；删除临时分支前再次确认提交已合入或补丁已等价落地。

## Sub-Agent 并行

- 仅对依赖明确且修改目录互斥的任务并行派发 Sub-Agent；禁止多个 Agent 同时修改同一文件或同一规格状态文件。
- 每个 Sub-Agent prompt 必须包含规格路径、任务编号、允许修改的文件范围、禁止事项、验证命令、是否允许提交及提交边界。
- Sub-Agent 运行 30-60 秒无输出时先查询状态或请求阶段性结果，不立即重复派发同一任务；确认卡死后再停止并重新派发。
- Sub-Agent 完成、失败或不再需要时立即关闭；集成前检查其提交范围、工作区状态和验证结果，不直接采信“已完成”摘要。

## 测试策略

- 默认采用 emulator-first：固定 Android API、system image、分辨率、语言、导航模式和快照，从干净状态运行，并记录设备指纹。
- 核心场景应支持连续运行和 flaky 统计；失败产物包含限额且脱敏的截图、UI hierarchy、日志时间窗、动作时间线和环境元数据，并在验收后按策略清理。
- 真实 App 测试必须固定 package、版本、ABI、来源和 SHA-256；第三方 APK 只放仓库外缓存，不自动抓取、重新分发或提交到 Git。
- 模拟器自动验收不能替代真机兼容性结论；真实 App 的广告、A/B 实验、模拟器检测和动态内容应作为兼容矩阵单独报告。

## 人工授权边界

- 仅可在 disposable emulator、debug/test 变体和明确 test-only 标记下自动预置测试服务、测试 Bridge 或无敏感数据 fixture；release 变体不得包含这些入口。
- 真机 USB/Wi-Fi ADB RSA、无障碍服务、MediaProjection、安装确认、未知来源、敏感运行时权限及 OEM 安全设置必须由用户完成，不得绕过或伪造已授权。
- 支付、购买、发送、账号登录、验证码、生物识别和系统安全设置不得因测试便利而自动确认。

## 设备操作纪律

- 每次设备操作必须使用明确 serial；多设备、设备离线或设备身份变化时立即停止，不隐式选择或切换目标。
- 动作前获取最新状态，说明目标包、类型化动作、预期效果和验证条件；每次只执行一步，动作后重新观察并验证后置条件。
- 坐标必须来自最新截图或 hierarchy；语义节点可用时优先语义动作。结果不明确、页面变化或动作失败时停止盲目重试并收集证据。

## 任务文档质量

- 新任务和路线图必须写明目标、具体步骤、依赖关系、可并行波次、允许修改范围、验收证据、建议提交边界及失败后的回滚或清理方式。
- 并行波次必须标出目录所有权和汇合点；共享协议、规格状态和导航文档由单一集成任务修改。
- 任务中的手动步骤必须说明为何无法安全自动化；可在模拟器自动化的步骤应提供 test-only 路径，减少重复人工确认。
