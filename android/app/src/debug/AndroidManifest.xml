<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name="dev.aiauto.android.accessibility.ScreenshotTestActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />

        <service
            android:name="dev.aiauto.android.accessibility.ScreenshotTestAccessibilityService"
            android:description="@string/test_accessibility_service_description"
            android:exported="true"
            android:label="Accessibility device test"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/test_accessibility_service_config" />
        </service>
    </application>
</manifest>
