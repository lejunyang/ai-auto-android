# N45 授权视觉接线任务

- [x] 审计 N44 lease、Provider action、Session Engine 与 N45 executor 边界
- [x] 创建独立 change spec/worktree
- [x] 增加 visual evidence parser 与无证据坐标 RED 测试
- [x] 引入 session-only planned action/context 生命周期
- [x] 实现授权 observation 到唯一 model candidate 的绑定
- [x] 实现 production screen/action/verifier 窄端口
- [x] 接入 N45 tap/long-click/swipe planner 并保持语义路径无回归
- [x] 覆盖停止、取消、确认等待、过期、漂移、secure 和清零
- [x] 运行定向、App 全量、lint、AndroidTest、comments、verify、diff-check
- [ ] 审查范围并提交

## 建议提交

`feat(android): bind authorized visual actions`

提交末尾保留且仅保留一次：

`Co-authored-by: TRAE CLI <noreply@bytedance.com>`
