# N42 验收清单

- [x] HTML 覆盖按钮、输入、长按、滚动、跳转、动态 DOM 和 iframe
- [x] Canvas 覆盖 tap、long-click、swipe、动画和状态结果
- [x] 三种模式可显式切换并可重置
- [x] 字体、资源、viewport、主题和数据完全离线固定
- [x] 非本地 URL 在发起网络前被拒绝
- [x] 状态可由 UI hierarchy 或本地测试接口机器读取
- [x] API 30、33、34 hierarchy、截图和状态断言一致
- [x] release 生产 App 不包含 fixture 调试入口
- [x] 定向测试、`make comments`、Android 构建和 `git diff --check` 通过
