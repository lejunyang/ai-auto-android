# N47 aactl Adapter 任务

- [x] 审计 aactl direct/Bridge、Runner 端口与 N31 lifecycle
- [x] 创建独立 change spec 并固定 visual 失败关闭边界
- [x] 先增加 identity、envelope、snapshot、action 和 lifecycle RED 测试
- [x] 实现固定 aactl identity、strict execFile 和单信封解析
- [x] 实现语义 observer、condition、route 与页面分类
- [x] 实现 Bridge semantic action 和 direct launch executor
- [x] 实现注入式 lifecycle、values 和 artifact adapter
- [x] 运行 adapter、N47、comments 与 diff-check 门禁
- [x] 审查允许范围并提交

## 建议提交

`test(lab): connect scenario runner adapters`

提交末尾保留且仅保留一次：

`Co-authored-by: TRAE CLI <noreply@bytedance.com>`
